/**
 * OffsetZap Main Entry Point
 * 
 * Imports and initializes the Across Protocol integration
 */

// Import our Across Protocol integration modules
import '../../../offsetzap/offsetzap-vite/src/across.js';
import '../../../offsetzap/offsetzap-vite/src/app-across.js';

// No need for additional initialization here as the app-across.js module
// has its own initialization logic that runs on DOMContentLoaded
