{
  "name": "offsetzap-toucan-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Bridge from Base to Toucan for carbon credit purchases",
  "devDependencies": {
    "hardhat": "^2.20.2",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "ethers": "^5.7.2"
  },
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network base"
  }
}